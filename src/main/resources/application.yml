spring:
  profiles:
    active: dev

  http:
    multipart:
      max-file-size: 50Mb
      max-request-size: 50Mb

mybatis:
  mapper-locations: classpath:public/mapper/*
  type-aliases-package: com.myproject.firstproject.entity

server:
  servlet:
    context-path: /firstproject
